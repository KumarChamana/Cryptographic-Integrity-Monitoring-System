# Cryptographic-Integrity-Monitoring-System
This project demonstrates secure message integrity verification using cryptographic hash functions such as MD5 and SHA-1. The system simulates message transmission between a source node and a target node and verifies whether the message has been altered during transmission.
